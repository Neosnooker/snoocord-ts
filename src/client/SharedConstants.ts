export const snoocord = Object.freeze({
  version: "0.1",
  repositoryUrl: "https://github.com/Neosnooker/snoocord",
});

export const discordUrls = Object.freeze({
  // Uses version 10 and JSON encoding
  wsGateway: "wss://gateway.discord.gg/?v=10&encoding=json",
});
